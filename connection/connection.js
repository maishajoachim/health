var typed = new Typed(".multiple-text", {
    strings: ["Heart rate(BPM)", "Blood Pressure(mmHg)",
    "Oxygen saturation(SPo2)",
     "Patient Confidances", "patient status"],
    typeSpeed: 100,
    backSpeed: 100,
    backDelay: 1000,
    loop: true,
})